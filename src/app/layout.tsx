import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "DocVerify IPPTI - Portal Verifikasi Resmi Terjemahan Tersumpah",
  description: "Portal verifikasi resmi Ikatan Penerjemah dan Pengalih Bahasa Tersumpah di Indonesia (IPPTI) untuk memvalidasi keaslian dokumen terjemahan tersumpah melalui kode QR dan bukti kriptografis.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
